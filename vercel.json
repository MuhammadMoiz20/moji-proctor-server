{
  "version": 2,
  "buildCommand": "npm install && npx prisma generate && npm run build",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "api/handler.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/handler"
    }
  ]
}
